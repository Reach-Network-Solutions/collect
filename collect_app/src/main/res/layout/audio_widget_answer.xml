<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="@dimen/margin_standard">

    <app.nexusforms.android.views.MultiClickSafeButton
        android:id="@+id/capture_button"
        style="@style/Widget.Collect.Button.WidgetAnswer"
        android:text="@string/capture_audio" />

    <app.nexusforms.android.views.MultiClickSafeButton
        android:id="@+id/choose_button"
        style="@style/Widget.Collect.Button.WidgetAnswer"
        android:text="@string/choose_sound" />

    <app.nexusforms.android.audio.NexusAudioControllerView
        android:id="@+id/audio_player"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"

      />
</LinearLayout>
